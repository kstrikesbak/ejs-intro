<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head') %>
    <body>
        <%- include('../partials/header') %>
        <div>
            <ol>
                <% users.forEach(({name, city}, idx) => { %> 
                    <li><%= name %> is from <%= city %>. </li>
                    <%})%>
            </ol>
        </div>
    </body>
</html>